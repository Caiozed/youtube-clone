<h1><%= @video.name %></h1>
    <%= video_tag @video.video_file.url, controls: true%>
<p><%= @video.description %></p>

<%= link_to "Delete", channel_video_path(params[:channel_id], @video), method: :delete, data: {confirm: "Are you sure ?"}%>
